# Q906

## Problem Statement

Implement the solution for Q906 using the provided code and required interface.

Class: `ReturnsAnalyzer`

## Required Interface

- `create_orders_df()`
- `create_returns_df()`
- `merge_orders_returns()`
- `category_refund_rate()`
- `high_return_sellers()`
- `clean_returns_data()`

## Notes

Preserve the required names, parameters, return values, and behavior expected by the assessment.
The solution source for this practice package is taken from the Pandas practice notebook practice notebook.
