# Q906

## Specification Status

No usable `specification.md` content was present in the supplied question package. This document is **derived directly from the solution code used for this practice question** and does not add requirements that are not represented by that code.

## Class

- `ReturnsAnalyzer`

## Required Methods

- `create_orders_df(self, order_data)`
- `create_returns_df(self, return_data)`
- `merge_orders_returns(self, orders_df, returns_df)`
- `category_refund_rate(self, merged_df)`
- `high_return_sellers(self, merged_df, n)`
- `clean_returns_data(self, returns_df)`

## Implementation Notes

Preserve the exact class name, method names, parameters, return values, and behavior represented by the solution code.

Source for this derived specification: `Pandas solution notebook`.
